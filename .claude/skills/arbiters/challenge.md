# Challenge — the doctrine moved to its own skill

The challenge doctrine lives in **`../arbiters-challenge/SKILL.md`** — read that
file now; it is the whole phase.

It was lifted out of this phase file into a topic-triggered skill on 2026-06-11,
because doctrine that loads only through the phase router never fires in
conversations *about* challenges — creating one, tuning one, asking "should this
be a challenge?". The skill's description is the firing surface; this pointer
stays so the walk's routing (`walk.md` → generative day → here) keeps working.

Nothing else lives here. One home, no drift.
