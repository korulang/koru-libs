# Generated by koru-vercel build — the deployable inputs are the compiled
# module (wasm/) plus the adapter (api/). Everything else stays out of the bundle.
api/**/*
!api/serve.mjs
wasm/*
!wasm/handler.wasm
