# Generated by koruc site.k build — the deployable inputs are the compiled
# module (wasm/) plus the adapter (api/). Everything else stays out of the bundle.
api/**/*
!api/serve.mjs
wasm/*
!wasm/handler.wasm
