// Streaming invariance: feeding "a" then "bc" through two update() calls must
// equal hashing "abc" in one shot. update BORROWS the handle (bare <hashing>),
// so the same `d` binding survives both chunks and the final call still proves
// the obligation was discharged.
//
// Expected: both lines print
//   ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad

~import libs/evp
~import std/io

~libs/evp:sha256.hex(input: "abc")
| digest h |> std/io:print.ln("oneshot = {{ h:s }}")

~libs/evp:sha256.init()
| ctx d |> libs/evp:update(d, bytes: "a") |> libs/evp:update(d, bytes: "bc") |> libs/evp:final.hex(d)
    | digest h |> std/io:print.ln("chunked = {{ h:s }}")
