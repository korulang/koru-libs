// Simple nested continuation test - no Source blocks

~import "$libs/sqlite3"
~import "$std/io"

~libs.sqlite3:open(path: ":memory:")
| db d |> libs.sqlite3:close(d.conn)
    | closed |> std.io:print.ln("Closed!")
| err _ |> std.io:print.ln("Open error")
