// Basic SQLite test - open and close

~import "$libs/sqlite"
~import "$std/io"

~libs.sqlite:open(path: ":memory:")
| db d |> libs.sqlite:close(d.conn)
    | closed |> std.io:print.ln("Opened and closed!")
| err _ |> std.io:print.ln("Error!")

pub fn main() void {}
