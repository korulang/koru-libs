// |js facet of rule_row_read_call_arg_js.k — the callees must be host-bodied
// for the defect to fire; with Koru subflows the same reads lower correctly.

~proc mk|js { return "x"; }
~proc show|js { console.log("row " + id + " " + label); }
