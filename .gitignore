# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Zig
zig-out/
.zig-cache/
a.out

# Koru / Zig temporary outputs
backend.zig
backend_output_emitted.zig
output_emitted.zig
build_output.zig

# Koru toolchain-generated build artifacts (regenerated on every build;
# contain machine-local paths + compiler module graph — never commit)
build.zig
build_backend.zig
compiler_env.zig
program.ast.json

# Agent worktrees (live git worktrees, not source)
.claude/worktrees/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE / Editor settings
.vscode/
.idea/
*.swp
*~
.fleet/
.helix/
.zed/

# Prose local data
.claude/prose/
# Prose skill (auto-generated)
.claude/skills/flash/
-e 
# Koru Studio sessions
.koru-studio/
